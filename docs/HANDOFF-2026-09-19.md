# HUAT FSAC 工作交接（2026-09-19 22:46 GMT+8）

> 给接手 mavis / 任何 Agent 看的一份自包含快照。
> 触发原因：本会话积分见底，需把 3 个 P1 任务继续推完。

---

## 0. 速览

| 任务                         | 优先级 | 状态                                                                       | 阻塞在哪              | Agent 能否继续                                              |
| ---------------------------- | ------ | -------------------------------------------------------------------------- | --------------------- | ----------------------------------------------------------- |
| T-036 部署：线上追平 main    | P1     | ✅ **已完成**（commit `788adcc`，已 push）                                 | —                     | 仅留 README/口径收尾                                        |
| T-021 release-please 自动化  | P1     | 🔀 **改路线**：人类不开组织开关且暂无法建 PAT → 走 C 无凭据应急（见 §3.4） | —                     | #85 保持 open；发版时 Agent 合并+补 tag+`gh pr create` 补建 |
| T-035 首页 logo.jpg 体积回归 | P2     | ✅ **已完成**（mavis，同日 23:2x，见 §4 末尾补记）                         | —                     | —                                                           |
| T-024/T-029/T-030            | P2     | 待办                                                                       | 需车队/实验室业务信息 | 等业务输入                                                  |

---

## 1. 当前 main 状态

- HEAD: `788adcc` (docs(workflow): mark T-036 done — agent-local deploy catches up with main)
- 上一 release: `v1.0.0` tag @ `e156e36`
- 滚动 Release PR: **#85** (`chore(main): release 1.1.0`) 状态 OPEN
- 线上 `https://huat-fsac.eu.org` 已追上 `main@788adcc`（curl CSP nonce 全绿）
- `wrangler.json:3` `account_id` = `bfdcbff6cfe16d2b9bd657593ba88f5f`（账号 `Iridite`）—— 本机 `%APPDATA%\xdg.config\.wrangler\config\default.toml` **已生成**（用户 22:42 跑的 `wrangler login`）

---

## 2. T-036 — ✅ 已完成（仅需留个口径修订，建议下次顺手做）

**已交付：**

- `pnpm deploy:worker` 跑通，`pnpm build` 11.40s 无 MDX 警告，`wrangler deploy` 上传所有 chunks 成功
- `curl -sI https://huat-fsac.eu.org/`：
    - `HTTP/1.1 200 OK`
    - `cache-control: private, no-cache, must-revalidate`
    - `strict-transport-security: max-age=31536000; includeSubDomains; preload`
    - `content-security-policy: nonce-p4-sbxjqpq3RaIzniFSSrg`
    - `server: cloudflare`
- `curl -s https://huat-fsac.eu.org/` 首页含 `news-date/news-text/news-item`、`data-nav-theme-toggle`、`mobile-nav` 抽屉、`contributors-section`（Kerwin 225+ / NekoRectifier 116+ / wsy-huat）、`<meta name="generator" content="Astro v7.3.2">` + `Starlight v0.41.11`
- docs/WORKFLOW.md §4 T-036 切 `已完成`，§7.4 加 mavis 22:46 一行
- commit `788adcc` 已 push 到 main

**遗留小事项（不阻塞，等下次随手做）：**

- AGENTS.md「发布与部署」段还残留旧"已 `wrangler whoami` 登录"口径——当前已确认只在当前 Windows profile 下成立，建议补一句"如换机器/重装系统需重跑 `wrangler login`"

---

## 3. T-021 — ❌ 原方案作废（人类不开组织开关）→ 改走"换身份"路线

> **⚠️ 2026-09-19 23:5x 更新（mavis）：** 用户明确**不会**开启组织开关，且无法创建 PAT。
> 本节 3.1/3.2 的指令**已失效，不要执行**——尤其是 3.2(c) 的 `gh pr close 85 -d`：**在当前状态下关闭 #85 会导致下一张 Release PR 永远无法自动生成**（新建 PR 需要 PAT/App 身份）。
> 现行方案见本节末尾「3.4 现行方案」。

### 3.1 （已作废）用户行为 —— 人类不开此开关

GitHub Org **HUAT-FSAC** → **Settings** → **Actions** → **General** → **Workflow permissions**：

> ~~✅ 勾选 **Allow GitHub Actions to create and approve pull requests**~~（人类已决定不开）

直接路径：https://github.com/orgs/HUAT-FSAC/settings/actions

### 3.2 （已作废）用户回话后 Agent 接手做的事

```bash
# ⚠️ 以下全部作废，保留仅作历史记录。特别是 (c) 关 #85 —— 现在关了就建不回来。
# (a) 复验组织策略已生效
gh api repos/HUAT-FSAC/Guidance-Astro/actions/permissions/workflow
# 期望返回: "can_approve_pull_request_reviews": true
#          "default_workflow_permissions": "write"

# (b) 触发一次 release-please（创建空 commit 不必要，直接 push 一个空 commit 即可；
#     或者 `gh workflow run release-please.yml -r main`）
gh workflow run release-please.yml -R HUAT-FSAC/Guidance-Astro
# 看最近 run 是否 success
gh run list -R HUAT-FSAC/Guidance-Astro --workflow=release-please.yml --limit 3

# (c) 关闭 PR #85（滚动 PR 的临时 workaround，已不再需要）
gh pr close 85 -R HUAT-FSAC/Guidance-Astro -d \
  -c "T-021 已解：组织开关开启，release-please 可自动建 PR；此滚动 PR 关闭，下一张将由 rp 自动创建"

# (d) 回写 docs/WORKFLOW.md
#     §4 T-021 行：状态 `阻塞` → `已完成`，负责人 `opencode/muse-spark` → `mavis`
#     §7.4 追加一条 mavis 的「完成 T-021 组织开关验证」
# 完成后：
git add docs/WORKFLOW.md
git commit -m "docs(workflow): mark T-021 done — org switch enables release-please auto-PR"
git push origin main
```

### 3.3 验收基线（按 3.4 现行方案重新定义）

- rp 工作流在每次 push main 后 `success`（已验证：run `35453263531` 在 `12e3af6` 上 success）
- **#85 保持 open 且被 rp 持续更新**（已验证：15:54 被更新）——这是当前唯一的 Release PR，不能关
- 需要发版时：合并 #85 → 由维护者补 `git tag` → 视 rp 是否自动发 Release 决定是否走 tag 触发的小 workflow → 用 `gh pr create` 从 rp 分支补建下一张

### 3.4 现行方案（2026-09-19 起）

组织开关只约束内置 `GITHUB_TOKEN`，换一个身份即可绕开。三条路线：

| 路线          | 做法                                                                                                                        | 状态                       |
| ------------- | --------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| A2 GitHub App | 建 App 装本仓（Contents:RW + PR:RW + Issues:RW）+ `actions/create-github-app-token` 铸短时令牌                              | 长期推荐，待人类可做时实施 |
| A1 细粒度 PAT | 仅本仓最小权限 PAT 存 `RELEASE_PLEASE_TOKEN`（workflow 已自动优先使用，零代码改动，最长 1 年需轮换）                        | 人类表示暂无法创建         |
| C 无凭据应急  | rp 用 GITHUB_TOKEN 仍可推分支/更新已有 PR/打 tag/发 Release，唯独"新建"被 403；故 #85 保持 open，必要时 `gh pr create` 补建 | **当前生效**               |

发版 runbook（零凭据，可由 Agent 执行）：

```bash
gh pr merge 85 -R HUAT-FSAC/Guidance-Astro      # 合并后 CHANGELOG/版本进 main
git tag v1.1.0 <merge-sha> && git push origin v1.1.0   # tag 不受分支保护约束
# 补下一张 Release PR：
gh pr create -R HUAT-FSAC/Guidance-Astro -B main \
  -H release-please--branches--main--components--huat-fsac-docs \
  -t "chore(main): release x.y.z" -b "由 rp 分支补建（组织策略禁 GITHUB_TOKEN 新建 PR）"
```

---

## 4. T-035 — 🟡 **进行中**（脚本已写，运行被 abort）

> **✅ 补记（mavis，2026-09-19 23:2x，本节已闭环）：**
> 排查发现 `logo.jpg` 实为 **1374x539 透明 PNG 误命名为 .jpg**（不透明像素仅 6.7%，深红字标压在主题相关的 header 底色上）。alpha 必须保留——压平成 JPEG 会在亮/暗主题其一产生黑/白底矩形，因此**偏离本节原方案**：兜底格式改为 PNG（不再是 logo.jpg），旧 `logo.jpg` 已删（母版迁至 `src/assets/logo-master.png`，不随构建发布，脚本幂等）。
> 实际产出：`logo.avif 13.5KB / logo.webp 24.2KB / logo.png 13.4KB`（三层全保留 alpha，512px 宽覆盖 2x DPR）；`Header.astro` + `LabApp.astro` 改 `<picture>` 三层 + `width=512 height=201`（顺带修正原 64x64 错误宽高比占位）。
> 门禁 lint/format/tsc/test:run 379/build/bundle/theme 全绿；`pnpm deploy:worker` 上线（Version `f416fadd`），线上 `/` 与 `/showcase-dashboard/` 已验 `<picture>` 三层 + 三资产 200 + 旧 logo.jpg 404 + CSP nonce。首页 logo 传输体积 **231KB → 13.5KB（-94%）**。
> §4.6 验收基线中「logo.jpg < 100KB / 线上含 logo.jpg」两条按上述原因作废，其余全部满足。

### 4.1 现状

- `public/assets/logo.jpg` 当前 231,335B（PR#83 / `481da26` 把 70,906B 提了上来）
- 实际显示尺寸：`src/components/overrides/Header.astro:22` `width="64" height="64"` 和 `src/components/showcase-lab/LabApp.astro:93`（无显式 width）
- 脚本 `scripts/quality/optimize-logo.mjs` 已写好（用项目已装的 `sharp@0.35.4`）
- 第一次 `node scripts/quality/optimize-logo.mjs` 被 abort（用户说 agent 积分见底）

### 4.2 继续干

```bash
cd D:\Coding\Projects\Guidance-Astro

# (1) 跑脚本（预计 < 5s）—— 会生成 logo.jpg (re-encoded) / logo.webp / logo.avif
node scripts/quality/optimize-logo.mjs
# 预期：3 个文件总计远 < 231KB
# 检查：
Get-ChildItem public/assets/ -Filter "logo*" | Select-Object Name, Length
# 应看到:
#   logo.jpg  ~30-60KB
#   logo.webp ~10-25KB
#   logo.avif ~5-15KB
```

### 4.3 改组件用 `<picture>`

#### `src/components/overrides/Header.astro:22`

把：

```astro
<img src="/assets/logo.jpg" alt="HUAT FSAC" class="header-logo" width="64" height="64" />
```

改为：

```astro
<picture class="header-logo-picture">
  <source srcset="/assets/logo.avif" type="image/avif" />
  <source srcset="/assets/logo.webp" type="image/webp" />
  <img src="/assets/logo.jpg" alt="HUAT FSAC" class="header-logo" width="64" height="64" loading="eager" decoding="async" />
</picture>
```

> 注意：原 `<img>` 有 `class="header-logo"`，迁移到 `<picture>` 后样式不会丢；如需精确控制 `<picture>` 自身样式，加一行 `header-logo-picture { display: inline-flex; }` 到对应的 `header-logo` 容器。

#### `src/components/showcase-lab/LabApp.astro:93`

同模板替换 `<img src="/assets/logo.jpg" ...>` 为 `<picture>` 三层。

> LabApp 里 `<img>` 没有 width/height —— 建议补一个 `width="64" height="64"` 防止 CLS。

### 4.4 验门禁

```bash
pnpm lint                      # 必须 0 error
pnpm format:check             # 0 error
pnpm tsc --noEmit             # 0 error
pnpm test:run                  # 379 全过
pnpm quality:bundle           # 图片预算 + bundle 预算仍过
pnpm quality:theme            # 0 error
pnpm build                    # 0 警告，dist/client/_astro/* 应见 logo.{avif,webp,jpg} 引用
```

### 4.5 部署 + 回写 + 关闭

```bash
# 部署
pnpm deploy:worker
curl -sI https://huat-fsac.eu.org/ | grep -i content-security-policy   # 期望含 nonce
curl -s https://huat-fsac.eu.org/ | Select-String -Pattern 'logo\.(avif|webp|jpg)'

# 回写 docs/WORKFLOW.md §4 T-035
# 状态 `待办` → `已完成`，负责人 `-` → `mavis`
# §7.4 追加一条 mavis 的「完成 T-035 logo.jpg 体积回归」

git add -A
git commit -m "perf(assets): shrink logo.jpg 231KB→~30KB + add webp/avif via <picture>"
git push origin main
```

### 4.6 验收基线

- `public/assets/logo.jpg` < 100KB（最好 < 60KB）
- `public/assets/logo.webp` 存在且 < 30KB
- `public/assets/logo.avif` 存在且 < 20KB
- 线上首页 HTML 含 `<picture>` 且 `<source type="image/avif">` + `<source type="image/webp">` + `<img src="/assets/logo.jpg" ...>`
- 所有门禁全绿

---

## 5. 不要做的事

- ❌ 不要碰 `wrangler.json` `account_id`（已配好）
- ❌ 不要把 deploy job 加回 `.github/workflows/ci-cd.yml`（已移除，`CLOUDFLARE_API_TOKEN` 仍缺）
- ❌ 不要改 `release-please.yml` `token: ${{ secrets.RELEASE_PLEASE_TOKEN || secrets.GITHUB_TOKEN }}`（已是 PAT 兜底形态）
- ❌ 不要 push 直接到 main 之外的分支（除非 T-035 改大想拆 PR——本任务纯改 public 文件 + 两个 .astro 组件，体积小可直接 main）
- ❌ 不要 `rm` 现有 logo.jpg — **已过时**：T-035 已确认它是误命名的透明 PNG，母版迁至 `src/assets/logo-master.png`，`public/assets/` 下现为 `logo.{avif,webp,png}` 三件（重跑 `pnpm assets:logo` 可再生成，幂等）
- ❌ **不要关闭 PR #85**（在无 PAT/App 的状态下，关闭或合并后不管都会让下一张 Release PR 无法自动生成）

---

## 6. 常用命令备忘

```bash
# wrangler
pnpm exec wrangler whoami                          # 看账号
pnpm deploy:worker                                 # = pnpm build && wrangler deploy --config dist/server/wrangler.json

# gh
gh api repos/HUAT-FSAC/Guidance-Astro/actions/permissions/workflow
gh run list -R HUAT-FSAC/Guidance-Astro --workflow=release-please.yml --limit 5
gh pr list -R HUAT-FSAC/Guidance-Astro --state open
gh secret list -R HUAT-FSAC/Guidance-Astro

# 项目内
pnpm lint && pnpm format:check && pnpm tsc --noEmit && pnpm test:run
pnpm build && pnpm quality:bundle && pnpm quality:theme
```

---

## 7. 联系 / 后续

- 本会话 ID: `mvs_8242bb39c1c64dcf9b644208d4eb0c7e`
- 接手 Agent: `mavis`（本仓库内 root session 即可，不必新建）
- 用户名 `kerwin`，主时区 GMT+8
- 协作日志接着 §7.4 末尾追加，不要插队
