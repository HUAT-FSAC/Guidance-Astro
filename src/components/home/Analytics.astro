---
/**
 * Umami 访问统计组件
 *
 * 使用说明：
 * 1. 前往 https://umami.is/ 或自建 Umami 实例
 * 2. 创建网站并获取 Website ID
 * 3. 将下方的 WEBSITE_ID 和 SCRIPT_URL 替换为您的值
 * 4. 在 index.mdx 中引入此组件
 */

// TODO: 替换为您的 Umami 配置
const WEBSITE_ID = "your-website-id"; // 替换为您的 Website ID
const SCRIPT_URL = "https://analytics.umami.is/script.js"; // 或您自建实例的地址

// 是否启用（设为 false 可禁用）
const ENABLED = false;
---

{ENABLED && <script defer src={SCRIPT_URL} data-website-id={WEBSITE_ID} />}

<!-- 
  替代选项：使用 Cloudflare Web Analytics（免费）
  如果您的网站托管在 Cloudflare Pages，可以直接在 Cloudflare 控制台启用
-->
