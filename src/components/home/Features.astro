---
import { Icon } from "@astrojs/starlight/components";
---

<div class="feature-grid">
    <a href="/2025/感知/" class="feature-card">
        <Icon name="rocket" size="2rem" color="var(--sl-color-accent)" />
        <div>
            <h3>🤖 算法部</h3>
            <p>感知 · 定位建图 · 规控 · 仿真</p>
        </div>
    </a>
    <a href="/2025/电气/" class="feature-card">
        <Icon name="seti:bicep" size="2rem" color="var(--sl-color-accent)" />
        <div>
            <h3>⚡ 电气部</h3>
            <p>线束 · 软件 · 硬件 · 电池箱</p>
        </div>
    </a>
    <a href="/2025/机械/" class="feature-card">
        <Icon name="setting" size="2rem" color="var(--sl-color-accent)" />
        <div>
            <h3>🔧 机械部</h3>
            <p>转向悬架 · 传动 · 车架 · 制动</p>
        </div>
    </a>
    <a href="/2025/项管/" class="feature-card">
        <Icon name="open-book" size="2rem" color="var(--sl-color-accent)" />
        <div>
            <h3>📋 项管部</h3>
            <p>新媒体 · 营销 · 运营管理</p>
        </div>
    </a>
</div>

<style>
    .feature-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2rem;
        margin: 3rem 0;
    }

    .feature-card {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        padding: 2.5rem 2rem;
        border-radius: 1.5rem;
        text-decoration: none !important;
        color: var(--sl-color-white) !important;
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        border: 1px solid rgba(255, 255, 255, 0.1);
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        position: relative;
        overflow: hidden;
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
    }

    .feature-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 1px;
        background: linear-gradient(
            90deg,
            transparent,
            rgba(255, 255, 255, 0.5),
            transparent
        );
        opacity: 0;
        transition: opacity 0.4s ease;
    }

    .feature-card:hover::before {
        opacity: 1;
    }

    .feature-card:hover {
        transform: translateY(-5px) scale(1.02);
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(255, 255, 255, 0.2);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
    }

    .feature-card h3 {
        margin: 0 !important;
        font-size: 1.5rem;
        font-weight: 700;
        letter-spacing: 0.5px;
    }

    .feature-card p {
        margin: 0 !important;
        font-size: 0.95rem;
        opacity: 0.7;
        line-height: 1.6;
    }
</style>
