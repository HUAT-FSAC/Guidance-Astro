---
/**
 * 自定义 MarkdownContent 组件
 * 增强文档内容区域，添加阅读进度条、编辑链接等辅助功能
 */
import Default from "@astrojs/starlight/components/MarkdownContent.astro";
import ReadingProgress from "../docs/ReadingProgress.astro";
import EditPageLink from "../docs/EditPageLink.astro";

// 检查是否是首页（不显示额外辅助组件）
const isHomePage = Astro.url.pathname === '/' || Astro.url.pathname === '';
---

{/* 阅读进度条 - 仅在文档正文页面显示 */}
{!isHomePage && <ReadingProgress />}

<div id="doc-content-wrapper" class="enhanced-content">
    <Default {...Astro.props}>
        <slot />
    </Default>

    {/* 编辑页面链接 */}
    {!isHomePage && <EditPageLink />}
</div>

<style>
    .enhanced-content {
        position: relative;
    }
</style>
