---
import LabApp from '../components/showcase-lab/LabApp.astro'
import type { ShowcaseLocale } from '../data/showcase-lab-i18n'

interface Props {
    locale?: ShowcaseLocale
}

const { locale = 'zh' } = Astro.props
---

<LabApp locale={locale} />
